# Project Notes & Open Decisions

**Last Updated:** 2026-08-01

1. ~~Confirm the threshold for SFace-standalone -- where did we get this? Can we get the same result? What configs were used?~~ **CLOSED 2026-08-02.** Original externally-supplied 1.030628 has no recoverable provenance: dataset build/config unknown, and no DL-track git history for it is reachable from current refs as of this session (the workspace root was restructured into a single repo on 2026-08-02; whether a fuller DL-side history existed before that and where it went was not established -- ask the original supplier if the exact provenance matters for the paper). Independently re-derived in-repo instead: `classical-cv/reports/independence/sface_lfw1/standalone_l2_yunet.json` (script: `classical-cv/scripts/archive/run_lfw_sface_standalone_independence.py`), YuNet, rank-165 unidirectional, N=5,749, 9.986 ppm FAR, `l2 = 1.0306278467178345` -- matches the supplied 1.030628 to 6 dp. Full family comparison (standalone/joint/deployed, LBPH+SFace): [`docs/independence/MASTER_FILE.md`](independence/MASTER_FILE.md) -- the durable reference. `classical-cv/src/hybrid/thresholds.json`'s new `_standalone_reference` block is a convenience copy only; that file is regenerated by `calibrate.py` and the block is not guaranteed to survive a re-run.
2. ~~Is the LBPH-standalone threshold the same for the LBPH-hybrid?~~ **CLOSED 2026-08-02.** Yes, by design decision, and now enforced: `cv_only`'s accept rule and the cascade's outright-accept rule are the identical single-engine test (`lbph_distance <= tau_accept`; SFace never runs before this gate either way), so there's no principled reason for them to diverge. The previous split (67.0084 standalone vs 77.769348 joint-hybrid-candidate) was traced to a box-crop-vs-full-frame harness bug in `src/hybrid/independence_test.py`, not a real detector- or pipeline-driven difference — confirmed by re-running the standalone box-cropped script on the joint test's own YuNet detector: lands at 67.033, next to the old Haar-standalone number, nowhere near 77.77. Checked against a whole-system-FAR-budget objection first (would tau_accept need tightening below 10 ppm to leave headroom for escalated-band leakage?) — ruled out, `docs/PAPER.md`'s LS-DB1 result shows the cascade's own combined rate already undercuts either engine run alone, so no budget-split rank-tightening is needed. `gate.tau_accept` is now `67.03325520645528`, same artifact `cv_only` uses (`reports/independence/lbph_lfw1/native_predict_scale_yunet.json`). Full trail: [`docs/independence/MASTER_FILE.md`](independence/MASTER_FILE.md), `classical-cv/docs/audits/STATE-08-02.md`.
3. Create and run the test to determine the `tau_reject` for the hybrid. **Scoped 2026-08-02, not yet run:** no new/hybrid-only script is needed — `tau_reject` (like `tau_accept`) is a single-engine LBPH-marginal-distribution question, since SFace never runs on a probe that gets rejected outright either. The existing standalone script already computes the full sorted unique-pair distance array and exposes `--unique-rank` as a CLI flag, so the 1%-FAR rank (165,226th unidirectional) is one rerun away: `run_lfw_lbph_native_predict_independence.py --unique-rank 165226 --output-json <new path>`. **But the deployed 88.4927 and the joint-hybrid candidate 88.492676 likely both carry the same box-crop harness-bug taint that `tau_accept`'s 77.769348 did** (identical full-frame `independence_test.py` run) — a plain re-run at rank 165226 on box-cropped YuNet would fix that. **Bigger open question, flagged by advisor, not yet resolved:** an impostor-tail rank may be the wrong criterion for `tau_reject` in the first place. Above `tau_reject` there's no false accept either way — the real cost of setting it is FRR on the escalation path (genuine probes denied a second SFace opinion) and escalation-rate/compute, not impostor tail rank. `docs/PAPER.md`'s cascade rescue numbers (SFace recovers 78.6% of LBPH's misses; degraded rank-1 5.10%→97.96%) and `robustness-protocol-map` §4's ~86-88 raw genuine-distribution EER point (right where 88.4927 currently sits) both depend on escalation actually happening — setting `tau_reject` from impostor rank alone risks silently cutting off the rescue path that produces the cascade's headline result. Needs a genuine-distribution-based (not impostor-tail) derivation before this item can close. **Analysis added 2026-08-02:** [`docs/experiments/tau_reject/THRESHOLD_ANALYSIS.md`](experiments/tau_reject/THRESHOLD_ANALYSIS.md) — genuine-distance percentiles by tier from an existing artifact (no new run needed), quantifies how badly the deployed 88.4927 currently guts the rescue path (51.09% of heavy-tier genuine probes hard-rejected pre-SFace), and a provisional suggestion (heavy-tier p99 ≈ 140.13) pending an unresolved impostor-side FAR check. Still open — not adopted.
4. With items 1~3 finished, determine the optimal hybrid SFace threshold. **Partially answered 2026-08-02, diagnostic only:** [`docs/experiments/hybrid_sface_threshold/ANALYSIS.md`](experiments/hybrid_sface_threshold/ANALYSIS.md), using item 3's provisional `tau_reject` (140.13, not canon). Finding: `cosine_genuine=0.363` is the dominant constraint on the escalation-band population, not `l2_genuine` — raising L2 from the deployed 1.0313 to this sample's zero-observed-FA ceiling (1.0417) buys under 1 point of genuine accept-rate. Not adoption-grade: band-conditioned sample (~69k) is far short of the 16.5M pairs behind the standalone sweeps, and the impostor side is a 1:1-protocol proxy for real 1-to-N escalation, not the event itself. Also surfaced an unplanned finding: at the new `tau_accept`, ~97% of ALL wild-LFW probes land in the escalation band regardless of which `tau_reject` candidate is used — the cheap LBPH-only fast path resolves almost nothing on wild LFW under these thresholds. Still open for a threshold-grade answer (needs a real joint 1-to-N run, not run here).
5. With item 4 finished, run the robustness test (hybrid)

Current state: 
The robustness test (standalone only) is NOT presentible to the adviser as of yet of yet

## Open Decisions for Tomorrow (2026-08-02)

### 1. Dual Reporting of Standalone LBPH Metrics in Final Thesis / Report
- **Issue:** Standalone LBPH exhibits **1.74% AR** under strict 1-to-N open-set gate threshold (`tau_accept = 67.0084`, $\sim 10\text{ ppm}$ FAR) vs. **65–75% Accuracy** on standard LFW 1:1 pairwise verification.
- **Option A (Cascade Focus Only):** Report only 1-to-N open-set identification numbers (1.74% AR clean, 0.91% DL41) to justify SFace cascade escalation.
- **Option B (Dual Baseline Reporting - Recommended):** 
  - Report 1:1 Pairwise Verification (65–75% Accuracy / EER) to benchmark against published classical baselines (Ahonen et al., 2006).
  - Report 1-to-N Open-Set Identification (1.74% AR @ 67.0084) to show operational gate performance under 10 ppm FAR constraints.
- **Reference Doc:** [`docs/experiments/IDENTIFICATION_VS_VERIFICATION.md`](file:///C:/Users/acer/Downloads/USLS%204th%20Year/Computer%20Vision/docs/experiments/IDENTIFICATION_VS_VERIFICATION.md)

### 2. Standalone LBPH Threshold Preservation
- **Status:** `tau_accept = 67.0084` remains frozen for standalone LBPH tests.
- **Note:** Do not alter or relax `67.0084` to `76.7922` in `thresholds.json` or standalone scripts, as `67.0084` is calibrated for native `predict_collect` scale on DB1 frontal-crop tolerances.
 of yet