# Project Notes & Open Decisions

**Last Updated:** 2026-08-01

1. ~~Confirm the threshold for SFace-standalone -- where did we get this? Can we get the same result? What configs were used?~~ **CLOSED 2026-08-02.** Original externally-supplied 1.030628 has no recoverable provenance: dataset build/config unknown, and no DL-track git history for it is reachable from current refs as of this session (the workspace root was restructured into a single repo on 2026-08-02; whether a fuller DL-side history existed before that and where it went was not established -- ask the original supplier if the exact provenance matters for the paper). Independently re-derived in-repo instead: `docs/results/independence_test/independence/sface_lfw1/standalone_l2_yunet.json` (script: `classical-cv/scripts/archive/run_lfw_sface_standalone_independence.py`), YuNet, rank-165 unidirectional, N=5,749, 9.986 ppm FAR, `l2 = 1.0306278467178345` -- matches the supplied 1.030628 to 6 dp. Full family comparison (standalone/joint/deployed, LBPH+SFace): [`MASTER_FILE.md`](results/independence_test/MASTER_FILE.md) -- the durable reference. `classical-cv/src/hybrid/thresholds.json`'s new `_standalone_reference` block is a convenience copy only; that file is regenerated by `calibrate.py` and the block is not guaranteed to survive a re-run.
2. ~~Is the LBPH-standalone threshold the same for the LBPH-hybrid?~~ **CLOSED 2026-08-02.** Yes, by design decision, and now enforced: `cv_only`'s accept rule and the cascade's outright-accept rule are the identical single-engine test (`lbph_distance <= tau_accept`; SFace never runs before this gate either way), so there's no principled reason for them to diverge. The previous split (67.0084 standalone vs 77.769348 joint-hybrid-candidate) was traced to a box-crop-vs-full-frame harness bug in `src/hybrid/independence_test.py`, not a real detector- or pipeline-driven difference — confirmed by re-running the standalone box-cropped script on the joint test's own YuNet detector: lands at 67.033, next to the old Haar-standalone number, nowhere near 77.77. Checked against a whole-system-FAR-budget objection first (would tau_accept need tightening below 10 ppm to leave headroom for escalated-band leakage?) — ruled out, `docs/PAPER.md`'s LS-DB1 result shows the cascade's own combined rate already undercuts either engine run alone, so no budget-split rank-tightening is needed. `gate.tau_accept` is now `67.03325520645528`, same artifact `cv_only` uses (`docs/results/independence_test/independence/lbph_lfw1/native_predict_scale_yunet.json`). Full trail: [`MASTER_FILE.md`](results/independence_test/MASTER_FILE.md), `classical-cv/docs/audits/STATE-08-02.md`.
3. ~~Create and run the test to determine the `tau_reject` for the hybrid.~~ **CLOSED 2026-08-02 — canonized, advisor sign-off.** `gate.tau_reject = 140.13` (was 88.4927, box-crop-harness-bug-tainted like the rejected `tau_accept` candidate). Method, not a plain percentile recipe: [`TAU_REJECT_METHOD.md`](results/independence_test/TAU_REJECT_METHOD.md) — an FRR-vs-escalation trade-off curve swept over `tau_reject` from 70-170, comparing genuine-hard-reject-rate against impostor-escalation-rate at each candidate. **Finding: no knee** — genuine and impostor escalation track almost 1:1 the whole way (LBPH doesn't separate genuine from impostor in this range on wild LFW). Given no candidate trades favorably, 140.13 (heavy-tier p99 genuine distance, [`THRESHOLD_ANALYSIS.md`](results/independence_test/thresholds/tau_reject/THRESHOLD_ANALYSIS.md)) was chosen as a deliberate permissive pick — makes the confident-reject branch functionally near-inert on wild LFW (stated explicitly, not hidden), favoring SFace escalation since LBPH can't be trusted to reject correctly here. Does not generalize to La Salle DB1. Full record: `docs/results/independence_test/MASTER_FILE.md` row 6.
4. ~~With items 1~3 finished, determine the optimal hybrid SFace threshold.~~ **CLOSED 2026-08-02 — resolved as: use the standalone value, no config change.** [`docs/experiments/hybrid_sface_threshold/ANALYSIS.md`](experiments/hybrid_sface_threshold/ANALYSIS.md), corrected post-advisor-review (the original diagnostic's cosine/L2 finding was backwards — see the doc's "Does raising L2_genuine" section). Reasoning: on wild LFW, ~97-99% of ALL probes land in the escalation band regardless of `tau_reject` candidate, so the band-conditioned SFace population is not meaningfully different from the marginal one — no hybrid-specific derivation is defensible. Three independent SFace-threshold derivations (joint-deployed 1.0313, standalone 1.0306278, band-conditioned zero-FA floor 1.0417) agree within ~1%, corroborating this. `L2_GENUINE_THRESHOLD` stays 1.0313. Scoped to LFW — does not hold on La Salle DB1 (`docs/PAPER.md`'s ~75%-kept-cheap figure implies a real, smaller escalation band there). Closes `docs/independence/MASTER_FILE.md` row 5.
5. ~~With item 4 finished, run the robustness test (hybrid)~~ **CLOSED 2026-08-02.** [`docs/experiments/hybrid-identification/README.md`](experiments/hybrid-identification/README.md) — full 1-to-N gallery/probe-disjoint LFW2 run on canon thresholds, all three modes. Mean cascade escalation across 41 mods: **97.51%** — confirms item 3/4's proxy-based prediction using the real 1-to-N `argmin` escalation event (not the 1:1 CSV proxy). `cv_only` AR 1.41% (41-mod avg), `dl_only`/`cascade` both 80.65%. Latency measured separately (isolated single-process run, no `--reuse-engine-scores`, N=172 probes) since the orchestrator's default run makes per-mode latency meaningless for `cascade` — see the doc for why and for the ~4.7h full-N cost estimate that ruled out an isolated full-scale run.

Current state: 
The robustness test (standalone only) is now presentable with a big finding: the TAR is not because of an error in the test, but rather
the actual performance of the models. Particularly, the LBPH performs rather poorly with LFW, even on clean probes. The problem lies with the model itself;
it was never meant for these kinds of things. Another source of the problem is the way we get the threshold: our (N*(N-1))/2 independence test only checks impostor pairs
and the false acceptance rate (FAR) requirement and never really checks the TAR.

Hypothesis: LBPH passed the algorithm test because the LSDB (the dataset it was trained and tested with) was more controlled.

Also done: finalizing the standalone SFace threshold (1.0306278467178345, item 1),
canonizing `tau_reject` (p99 heavy tier, **140.13**, item 3, advisor sign-off), and
resolving the hybrid SFace threshold question as "use standalone, no config change"
(item 4) since the escalation band ≈ the marginal population on LFW.

Using the canon tau_reject value, ~97-99% of ALL probes (genuine and impostor alike)
escalate on wild LFW -- most probes skip the fast path. Confirmed via a full
FRR-vs-escalation trade-off curve (`docs/independence/TAU_REJECT_METHOD.md`): there's
no tau_reject value in the 70-170 range where LBPH meaningfully separates genuine from
impostor, so this isn't a threshold-tuning problem -- it's a stated, permanent property
of LBPH-on-wild-LFW that the report should state plainly rather than tune around.

## Open Decisions for Tomorrow (2026-08-02)

### 1. Dual Reporting of Standalone LBPH Metrics in Final Thesis / Report
- **Issue:** Standalone LBPH exhibits **1.74% AR** under strict 1-to-N open-set gate threshold (`tau_accept = 67.0084`, $\sim 10\text{ ppm}$ FAR) vs. **65–75% Accuracy** on standard LFW 1:1 pairwise verification.
- **Option A (Cascade Focus Only):** Report only 1-to-N open-set identification numbers (1.74% AR clean, 0.91% DL41) to justify SFace cascade escalation.
- **Option B (Dual Baseline Reporting - Recommended):** 
  - Report 1:1 Pairwise Verification (65–75% Accuracy / EER) to benchmark against published classical baselines (Ahonen et al., 2006).
  - Report 1-to-N Open-Set Identification (1.74% AR @ 67.0084) to show operational gate performance under 10 ppm FAR constraints.
- **Reference Doc:** [`docs/experiments/IDENTIFICATION_VS_VERIFICATION.md`](file:///C:/Users/acer/Downloads/USLS%204th%20Year/Computer%20Vision/docs/experiments/IDENTIFICATION_VS_VERIFICATION.md)

### 2. Standalone LBPH Threshold Preservation — SUPERSEDED 2026-08-02
- **Status:** `tau_accept = 67.0084` is no longer the value — unified with the hybrid gate 2026-08-02, see item 2 above. Canon value is now **`67.03325520645528`**, frozen for both standalone LBPH tests and `gate.tau_accept`.
- **Note:** Do not alter or relax `67.03325520645528` to `76.7922` (LFW 1% FAR diagnostic point, unrelated) in `thresholds.json` or standalone scripts — the box-cropped native `predict_collect` scale calibration reasoning still applies, only the specific digits moved (67.0084 → 67.03325520645528, same derivation method, different detector — see item 2's full record).
