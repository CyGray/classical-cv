# Pairwise-to-Mainline Manuscript Lineage

This directory began as an experimental fork separate from the main
manuscript-version sequence. By the final advisor decision,
`014p_finalizations.docm` is now promoted as the exact approved baseline for
the next main-line canonical self-match robustness manuscript successor. The
approved baseline remains untouched in this directory; the promotion selects
its lineage and does not claim that the successor has already been created.

- Baseline: docs/manuscript/versions/012_lsface_gate-accept-protection-descriptive.docm
- Fork: 012p_lsface_legacy-selfmatch-robustness.docm
- Historical intermediate: 013p_lsface_legacy-selfmatch-robustness-finalized - Copy.docm
- Approved successor baseline: 014p_finalizations.docm
- Predecessor fork: 016p_new.docm
- Focused cleanup fork: 017p_lsface_focused-cleanup.docm (with 017p_lsface_focused-cleanup.pdf)
- Protocol: one LFW source image per identity was both enrolled and used as
  the source of every clean/augmented probe.
- Interpretation: the reported values are same-image transform-sensitivity
  retention, not image-disjoint recognition accuracy, pairwise verification,
  or a FAR measured by this experiment. Table 5 separately carries historical
  threshold-basis FAR values for its standalone rows; those come from distinct
  LFW impostor calibrations rather than this protocol.
- Historical source: classical-cv/docs/audits/STATE-08-01.md, including the
  recorded 86.66% LBPH, 98.22% SFace, and 94.69% cascade retention results.

## Current approved formatting and content source

`014p_finalizations.docm` is the exact approved formatting and content source
for the next main-line canonical self-match robustness manuscript successor.
Start that future successor from this file unless the user explicitly names a
different baseline, preserving its layout, styles, content, and VBA project.
Do not move or alter `014p_finalizations.docm` merely because it has been
promoted; it remains the baseline in this directory until the successor is
actually created.

The earlier `012p` and `013p` artifacts remain unchanged historical lineage
records; do not delete them or reinterpret their contents. This baseline
promotion governs manuscript lineage only. The underlying protocol remains
same-source transform sensitivity, not image-disjoint recognition, pairwise
verification, or a FAR measurement by this experiment.
