Canonical algorithm-selection evidence and supporting method notes for:
- classical models (LBPH, Eigenfaces, Fisherfaces)
- deep-learning models (SFace, ArcFace, FaceNet)

`lsdb_selection_dl_trio_2026-08-11/` is canonical fresh DL-only comparison on
exact LSDB selection split. It gives each candidate fresh enrolment,
calibration, and held-out testing; use it for DL selection claim.

`lsdb_selection_dl_2026-08-11/` is earlier SFace-only precursor. Retain it as
audit evidence, but do not cite it as completion of DL comparison.

Check each artifact's manifest status before citation. The preprocessing PNGs
are illustrations, not headline measurements; `fig1_tar_far_roc.png` needs a
verified rerun/redraw before camera-ready manuscript use.
