1. I added light and medium modifications on the test and train datasets to augment it for better training and testing results
2. I pointed the defaults and args to the split and augmented split (except for raw)
3. I retrained and re-evaluated all models using the ff setup: processed + light + medium for training, and processed test split (no aug)
4. I did 60 second sessions for each model to evaluate avg fps